# AGENTS.md

## 项目说明

当前项目是南网 e 卡消息中心前端。

技术栈：

* Vue 3
* Vite
* TypeScript
* Element Plus
* Vue Router
* Pinia
* Axios
* SCSS

## 代码规范

1. 使用 Vue 3 Composition API 和 `<script setup>`。
2. 使用 TypeScript，不随意使用 `any`；确实无法避免时必须进行类型收窄。
3. 页面和组件使用 PascalCase 命名。
4. 普通变量、方法和组合式函数使用 camelCase 命名。
5. 接口请求统一放在 `src/api`。
6. 类型定义统一放在 `src/types`。
7. 公共组件统一放在 `src/components`。
8. 页面统一放在 `src/views` 对应模块目录。
9. 路由统一在 `src/router` 管理。
10. Axios 请求和响应处理统一复用 `src/utils/request.ts`，不得在页面中重复创建 Axios 实例。
11. 页面优先使用 Element Plus，不重复开发已有基础组件。
12. 样式优先使用 SCSS，不在模板中堆积大量内联样式。
13. 核心组件和复杂交互可以添加简洁中文注释，不写无意义注释。
14. 页面图标优先使用 @element-plus/icons-vue，不得使用 emoji、字符图标或重复绘制 SVG；仅在图标库无合适图标时使用自定义图标。

## 小型问题直接执行

对于文案、样式、输入控件、单个组件或明确报错：

1. 不先输出详细执行计划，直接定位并修改。
2. 最多检查用户指定文件及其直接依赖。
3. 不读取完整 OpenAPI、需求文档或其他模块。
4. 不使用 Browser，除非用户明确要求。
5. 不进行额外架构、并发、性能或全局回归检查。
6. 前端修改完成后默认只执行 `npm.cmd run build`。
7. 完成说明不超过 5 行。


## 数据与接口规范

1. 接口字段以后端实际响应和 `reference/openapi.json` 为准，不自行修改字段含义。
2. 原型中的静态数据只用于样式参考，不能作为接口契约。
3. 所有业务 ID，包括场景、参数、渠道、模板和单位 ID，统一使用 `string`。
4. 禁止对业务 ID 使用 `Number`、`parseInt`、`parseFloat` 或一元加号转换。
5. `status` 使用数字 `0` 和 `1`，不得向后端提交 boolean。
6. 处理 `status=0` 时不得使用会覆盖合法零值的逻辑或，例如不得使用 `status || 1`。
7. 优先使用空值合并，例如 `status ?? 1`。
8. 分页数量、统计数量、优先级等保持 `number`。
9. VO 字段可能为空时必须提供合理的空值处理。
10. 不在代码中写死生产接口地址、账号、密码、Token 或密钥。

## 页面交互规范

1. 页面必须处理加载中、空数据、请求失败和提交中状态。
2. 表单提交必须防止重复点击。
3. 列表中的异步操作必须防止同一操作重复提交。
4. 查询条件重置后应恢复第 1 页。
5. 页面宽度不足时，表格优先允许横向滚动，不强行压缩重要字段。
6. 修改页面时不得破坏现有接口、分页、路由和表单逻辑。

## 任务执行范围

1. 每次只实现用户明确要求的当前任务，不提前开发后续模块。
2. 开始修改前只定位与当前任务直接相关的文件。
3. 除非用户明确要求，不扫描整个项目。
4. 不读取和当前任务无关的需求文档或 OpenAPI 内容。
5. 不修改与当前任务无关的文件。
6. 不主动进行额外重构、架构调整、性能优化或并发审查。
7. 发现无关问题时只在结果中说明，不顺带修改。
8. 当前 Codex 只能访问前端工作区，不尝试读取后端项目目录。
9. 不创建与当前需求无关的空组件、空接口或占位架构。

## 模块限制

1. 当前不开发登录、用户、角色、权限和动态菜单功能。
2. 当前不引入完整若依项目。
3. 不开发完整组织机构管理模块。
4. 任务明确要求时，可以使用独立、可替换的开发环境模拟单位树。
5. 模拟数据必须与页面业务代码解耦，不得伪装成真实后端接口。
6. 不引入新依赖，除非用户明确同意。

## 浏览器与本地服务

1. 除非用户明确要求，不启动浏览器进行验证。
2. 除非用户明确要求，不主动启动长期运行的前端开发服务。
3. 用户已经启动本地服务时，直接使用其提供的 localhost 地址。
4. 本地服务无法访问时，明确说明浏览器验证未完成。
5. 不改用未经用户同意的浏览器、服务启动或代理方案。
6. 浏览器验证只检查本次修改涉及的页面和交互。

## 测试与构建

1. Windows PowerShell 环境统一使用 `npm.cmd`。

2. 修改完成后执行：

   `npm.cmd run build`

3. 如果项目存在 lint 命令，并且本次任务明确要求 lint，再执行：

   `npm.cmd run lint`

4. 除非用户明确要求，不新增单元测试、组件测试、E2E 测试或测试数据文件。

5. 不得删除、跳过或弱化已有测试、构建检查和 TypeScript 校验。

6. 小型文案、样式和局部交互修改默认只执行构建，不额外启动浏览器。

7. 接口、路由或复杂业务交互修改，在已有本地服务可用时再进行相关浏览器验证。

8. 不处理与本次修改无关的历史构建警告。

## Git 与文档

1. 不自动执行 Git 提交。
2. 不自动推送远程仓库。
3. 不修改 README，除非用户明确要求。
4. 不提交 `reference` 中仅供本地开发使用的临时文件，除非用户明确要求。

## 完成说明

完成后保持简洁，只说明：

1. 修改了哪些主要文件。
2. 完成了哪些需求。
3. 执行了哪些构建或验证命令。
4. 构建和验证是否通过。
5. 是否存在确实未完成或未验证的问题。

不要重复完整需求，不输出冗长的自我检查过程。
